<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    tools:context=".fragment.CategoriaDialogFragment">

    <FrameLayout
        android:id="@+id/flCategoria"
        android:layout_width="match_parent"
        android:layout_height="480dp"
        android:layout_marginBottom="16dp"
        android:layout_alignParentTop="true">

        <android.support.v4.widget.SwipeRefreshLayout
            android:id="@+id/swipeToRefreshCategoria"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/recyclerViewCategoria"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:cacheColorHint="@android:color/transparent"
                android:clipToPadding="false"
                android:divider="@null"
                android:dividerHeight="0dp"
                android:listSelector="@android:color/transparent"
                android:scrollbarStyle="outsideOverlay"
                android:scrollbars="vertical" />

        </android.support.v4.widget.SwipeRefreshLayout>

    </FrameLayout>

    <Button
        android:id="@+id/btnCancelarCategoria"
        style="?android:textAppearanceSmall"
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/flCategoria"
        android:background="@drawable/btn_entrar_gradient"
        android:minHeight="48dp"
        android:text="@string/cancelar"
        android:textColor="@color/mint_cream"
        android:textStyle="bold" />

</RelativeLayout>